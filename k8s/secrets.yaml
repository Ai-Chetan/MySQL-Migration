# Template for secrets - DO NOT COMMIT ACTUAL SECRETS
# Use kubectl create secret or external secret manager
apiVersion: v1
kind: Secret
metadata:
  name: migration-secrets
  namespace: migration-platform
type: Opaque
stringData:
  db-password: "REPLACE_WITH_ACTUAL_PASSWORD"
  jwt-secret: "REPLACE_WITH_ACTUAL_JWT_SECRET"
  vault-master-key: "REPLACE_WITH_ACTUAL_VAULT_KEY"
  s3-access-key: "REPLACE_WITH_ACTUAL_S3_KEY"
  s3-secret-key: "REPLACE_WITH_ACTUAL_S3_SECRET"
